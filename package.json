{
  "name": "ess-matcher",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on transaction review matcher for ESS wrong amount rows.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "check": "node --check src/app.js && node --check src/browser.js && node --check src/config.js && node --check src/historyStore.js && node --check src/pageActions.js && node --check src/worker.js"
  }
}
